export type KlineRaw = {
  start: string;
  open: string;
  high: string;
  low: string;
  close: string;
};

export type Kline = {
  start: number;
  open: number;
  high: number;
  low: number;
  close: number;
};
